import * as React from "react";
import { Head, Main, NextScript } from "next/document";

export default (): JSX.Element => (
  <html>
    <Head />
    <body>
      <Main />
      <NextScript />
    </body>
  </html>
);
